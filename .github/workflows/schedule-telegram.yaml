name: telegram_scraping

on:
  schedule:
    - cron: "0 */1 * * *"


jobs: 
  telegram_scraping_job:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v3
    - uses: r-lib/actions/setup-r@v2
    - name: Telegram monitor
      run: Rscript source(R/telegram_monitor.R)