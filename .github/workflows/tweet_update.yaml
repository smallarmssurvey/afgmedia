name: Update tweets

on:
  # schedule:
  # - cron: "0 8 * * *"
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - uses: r-lib/actions/setup-r@v2
        with:
          use-public-rspm: true
        
      - name: Update data
        run: |
          source("R/tweets_download.R")
        shell: Rscript {0}
